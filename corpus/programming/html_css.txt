HTML (HyperText Markup Language) is the standard markup language for web pages. CSS (Cascading Style Sheets) is a style sheet language used for describing the presentation of a document written in HTML.